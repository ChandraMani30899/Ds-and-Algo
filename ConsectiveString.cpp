#include<bits/stdc++.h>
using namespace std;

string addStar(string str)
{
       if(str.length()==0)
            return str;

    char c=str[0];
    string sub=str.substr(1);
    string rec_str=addstart(sub);

    if(rec_res[0]==ch)
    {
        return ch+"*"+rec;
    }
    else{
        return ch + rec;
    }

}
