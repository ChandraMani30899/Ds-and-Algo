#include <bits/stdc++.h>
using namespace std;
int subarraySumWithK(int arr[], int n.int k)
{
    unordered_map<int, int> h;
  int count=0;
   int max=INT_MIN,min=INT_MAX;
    for (int i = 0; i < n; i++)
    {
        h[a[i]] = true;
    }

    return count;
}